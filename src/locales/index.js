// eslint-disable-next-line import/extensions
import al from './al';
// eslint-disable-next-line import/extensions
import en from './en';
// eslint-disable-next-line import/extensions
import ch from './ch';

export default { al, en, ch };
